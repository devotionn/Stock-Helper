target-version = "py311"
line-length = 120

[lint]
# 现有项目尚未统一格式化；CI先强制执行会影响正确性的语法、名称和控制流检查。
select = ["E4", "E7", "E9", "F"]
ignore = ["E501", "F401"]

[lint.per-file-ignores]
"__init__.py" = ["F401"]
